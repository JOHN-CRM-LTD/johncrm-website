# Chat exit implementation plan

Execute inline as requested.

1. Add exit timing coverage to `tests/chat-demo-motion.test.mjs`: reading interval, late fade, complete exit, reverse scroll.
2. Add `getChatExitState` to `chat-demo-motion.ts`, using section-bottom geometry and a smooth late fade.
3. Update the existing scroll callback in `ChatDemo.tsx` to publish exit CSS variables using untransformed section geometry.
4. Compose the outward translation with the existing entrance in `section-motion.css`, limiting flow-mode exit to the final scenario. Keep reduced motion static and preserve clipping.
5. Run focused tests, all existing Node tests, typecheck, and a Vite build into a temporary directory. Use existing Playwright outside the repo to inspect desktop/mobile scroll positions, full exit, reversal, reduced motion, and console health. Preserve existing dirty source work. The existing build test rebuilds dist; refresh it from the final source afterward.

Completed: 17 focused motion tests, typecheck, and production build pass. Playwright at 1440x900 and 390x844 confirms opposite slide directions, late fade, zero opacity and fully offscreen geometry at the end, reverse restoration, no horizontal overflow, and no console/page errors. Reduced motion remains stationary and opaque. The full suite reports five unrelated source-shape/copy failures in App.tsx checks (logo placement, hero weight, legal navigation, poster copy, pricing copy).
