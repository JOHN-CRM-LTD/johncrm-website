# Revised hero exit implementation plan

Execute inline as requested.

1. Wrap left hero content in one moving inner group inside its stationary grid cell. Keep the portrait in its own moving wrapper; remove the scan layer and line peel wrappers.
2. Reuse the geometry helper and MotionPage scheduling. Apply one progress variable per column and remove the obsolete scan variable.
3. Translate content left and portrait right in hero-exit.css. Reverse the intro masks using content progress. Keep reduced-motion and focus fallbacks.
4. Run focused tests and typecheck, build outside tracked dist, and verify desktop/mobile displacement, masks, reverse scroll, focus and reduced motion in Chromium.
