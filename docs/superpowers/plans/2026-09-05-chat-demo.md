# Chat demo implementation

1. Create typed EN/CN/HK copy and four product-grounded sample conversations in `src/app/components/chat-demo-content.ts`.
2. Build accessible conversation cards with industry names that type into the headers. Remove extra copy, composers, context cards, playback controls, and outcome labels. Extract pure scroll-state helpers in `chat-demo-motion.ts` and cover fast scroll, progressive reveals, upward transitions, reverse scrolling, and clamping with focused tests.
3. Use passive scroll updates to reveal messages, preserve a reading interval, and slide each conversation upward into the next. Keep normal-flow examples on mobile and short screens, with complete static conversations for reduced motion.
4. Style taller cards, the black SEAMLESS DELIVERY heading, and crimson insurance, blue/yellow retail, green real estate, and plum/peach hospitality themes in `src/styles/chat-demo.css`. Preserve the existing typography, grid, and App placement between Features and Poster.
5. Run `npm run typecheck`, `npm run build`, `node --test tests/*.test.mjs`, and `git diff --check`. Inspect scroll transitions, quick scrolling, navigation, themes, and responsive card geometry in the browser. Preserve source changes only; do not publish.
