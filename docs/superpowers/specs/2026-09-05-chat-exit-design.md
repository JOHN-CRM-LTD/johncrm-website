# Chat comparison exit

After the final hospitality messages have appeared, scroll the Standard Chat panel left and JOHN CRM panel right. Tie the exit to the bottom of the chat section approaching the viewport: start at 1.5 viewport heights and finish at .85, as the next poster section enters. Keep panels opaque for the first 60% of the exit, then smoothly fade to zero. Travel beyond the viewport edges, clip horizontal overflow, and reverse naturally when scrolling up.

Apply to the pinned comparison and only the last comparison in the responsive flow. Flow layouts start later, at .9 viewport heights, and finish at .25 so the final messages have time to enter before exiting. Preserve message timing, entrance motion, copy, layout, and the static reduced-motion experience. Validate desktop, mobile, reverse scrolling, and reduced motion with browser geometry and screenshots.
