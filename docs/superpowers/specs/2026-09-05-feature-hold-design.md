# Performance card reading interval

The six cards enter with the top three moving from left to right and the lower three from right to left. Once entered, each card rests at its exact original transform while the page continues scrolling normally. Both rows exit together when the stationary Seamless Delivery heading wrapper reaches its reveal threshold (98% of viewport height). Top cards exit right; lower cards exit left. Smooth easing joins entry, hold, and exit without jumps. Reverse scrolling retraces the sequence.

Preserve copy, layout, curved travel, opacity, divider fading, navigation alignment, and reduced-motion behavior. Use measured heading geometry so the trigger adapts to viewport and language. Implement inline without approval or commits per the user's instructions.
