# Curved section reveals

Extend the current scroll clock and preserve the authored layout, copy, grid, and chat wireframes. The performance heading enters from the left; cards 1–3 enter from the right and cards 4–6 enter from the left. Entrance and exit follow one continuous shallow arc with a stationary reading interval between them. Complete entrances earlier than the current lift so content stays settled longer. Reverse scrolling retraces the same motion.

Use stationary measurement wrappers and translate their contents to avoid transformed geometry feeding back into scroll progress. Reduce travel on narrow screens; reduced motion and keyboard focus show the content immediately. Reuse the existing hero black-bar reveal on all main landing headings, including Seamless Delivery, with scroll progress controlling subsequent headings. Preserve the hero's timed opening.

Verify direction, curved continuation, stable reading interval, reverse scroll, desktop/mobile geometry, heading wipes, reduced motion, typecheck, existing tests, and production build. Execute inline without approval or commits as requested.

## Follow-up: fluid scroll motion

The user's follow-up supersedes the stationary reading interval: keep a slow continuous drift through the readable portion of the curve, preserving the established directions and heading wipe. Fade the six blocks in and out, beginning their exit fade while still visible. Fade the existing grid dividers as the grid top crosses from 20% below the viewport top to 20% above it; reverse scrolling restores them. Preserve grid geometry and reduced-motion behavior.
