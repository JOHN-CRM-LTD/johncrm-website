# Animated chat comparison

Add a self-contained section immediately after Features and before Poster. Use the supplied comparison as a composition reference, preserving this site's Barlow Condensed headings, Inter UI text, fine rules, grid, and neutral surfaces. The interface stays real HTML and CSS.

The heading is “SEAMLESS DELIVERY” in black. Two taller conversation windows contrast standard chat with JOHN CRM. Keep only the heading, conversation cards, and industry navigation. Remove composers, replay/pause, labels above the cards, context cards, supporting copy, and bottom outcome labels. Card headers read “Standard Chat - Retail” and “JOHN CRM - Retail”, with the matching industry name.

Use crimson #b40e3a for insurance, blue #0253e3 with yellow #ffc221 accents for retail, forest green #176b50 with sage accents for real estate, and plum #75458c with peach accents for hospitality. Preserve the neutral standard-chat treatment and use themed headers, outgoing bubbles, fine patterns, and subtle illumination on the JOHN CRM side.

Four examples: insurance policy questions and adviser handoff; retail returns using a store policy; property enquiries using listing information and agent handoff; hospitality requests using guest information and team handoff. These are illustrative knowledge-grounded replies, customer context, and human handoffs supported by the current CRM; avoid implying guaranteed claims decisions or unconfigured transactional integrations.

Desktop: hold the comparison in view while natural scrolling directly reveals messages in four industry chapters. Messages become fully visible early in each chapter, followed by a reading interval. During the final 18% of a chapter, the old bubbles move upward and the next conversation enters from below; the industry name types into the fixed card header. Fast scrolling immediately displays the corresponding conversation state, without waiting for playback timers. Industry buttons land on a completed conversation, and a thin rail tracks scroll. No wheel interception. Short and mobile screens show all four examples in document flow with scroll-driven reveals, stacking the two taller cards on phones. Reduced motion shows completed conversations without typing or pinned scrolling. English, Simplified Chinese, and Traditional Chinese follow the existing language selector.

Implementation: isolated ChatDemo component, typed localized scenario data, pure scroll-state helpers, one scoped stylesheet, and an App insertion. Use existing React and Lucide dependencies. Clean up observers, scroll listeners, and animation frames. Check scroll reveals, upward transitions, chapter buttons, themes, language switching, mobile geometry, reduced motion, build, typecheck, and the existing node tests. No changes to the CRM checkout or publishing.
