# Hero reverse wipe and outward exit

The revised direction replaces the line peel and portrait scan closure. Reverse the headline's existing black-bar entrance: black rectangles cover each line from right to left as the entire left content group slides left. The headline, description, Get Started action and support note share a single transform. John slides right intact, without scaling, cropping or a scan line.

Reuse MotionPage's scroll controller and original entrance masks. Measure stationary outer column wrappers. Mobile columns use their own positions, keeping John intact until his exit region. Reverse scrolling restores all content. Reduced motion and keyboard focus retain readable static content and usable actions. Preserve wording, localization, layout and the ASCII cursor lens.

Verify focused geometry/portrait tests, typecheck, isolated build, desktop/mobile scrolling, complementary black-bar/text masks, shared leftward displacement, rightward portrait movement, focus and reduced motion.
